Работа с Unix.
Как создать директорию?

1. Mkdir dir_name

Перейти в директорию?

1. Cd path

Создать в директории пустой файл?

1. Touch test

Вывести содержимое файла в stdout?

1. Cat file_name

Как потоково выводить в консоль новые строки, которые в это время динамически добавляются в конец файла?

1. Tail -f example.log

Как вывести в консоль последние 15 строк файла?  

* tail -n 15

Как посмотреть список файлов в директории?

1. Ls -al

Какой командой можно создать символическую ссылку на файл?

* ln -s file link

Что такое stdin, stdout, stderr?
stdin - стандартный поток ввода
stdout - стандартный поток вывода
stderr - стандартный поток вывода сообщений об ошибках

Работа с базами данных.  

Работал ли с SQL базами? Какие основные операции с SQL базами вы знаете?
Работал, SELECT INSERT UPDATE DELETE TRUNCATE DROP JOIN

Что такое транзакция?
последовательность операций, выполняемых как единое целое

Работа с GIT. Какие есть операции при работе с GIT?
Git Commit
Git push
Git pull
Git checkout
Git merge
Git status
Git log
Git cherry-pick
git fetch

Вопросы относятся к command line интерфейсу:

Как создать ветку локально?
git branch name
Как создать ветку локально на основе удалённой ветки?
git checkout -b branch_name origin/name
Как подтянуть изменения из удалённой ветки в git? git pull

Общие вопросы

Что такое HEAD в git
указывает на место  где вы находитесь в дереве коммитов

Работа с Docker.
Работал ли с Docker?
Да

Вопросы относятся к command line интерфейсу:

Как посмотреть список всех локальных образов?
docker images

Как посмотреть список запущенных контейнеров?
docker ps
Команда запуска контейнера?
docker run -d --name my-container my-image
Какая команда в Dockerfile отвечает за создание слоя из другого образа?
 FROM
Какой командой можно переключиться в контейнер для выполнения команд в нём?
docker exec -it CONTAINER_NAME /bin/bash

Тесты. Приоритет MEDIUM

Писал ли тесты?
Да

На каких фреймворках писал тесты в Python? Перечисли какие-нибудь из них?
Purest
Unittest
Gherkin

В автотесте: как называется операция сравнения результата с ожидаемым значением?
Assert

Как в Python юнит-тесте протестировать приватный метод класса?
Вообще можно достучаться даже до приватных методов в питоне.

Backend

Какие существуют HTTP методы?
GET
POST
PUT
DELETEOPTIONSPATCH
HEAD

Какие коды ответов HTTP-сервера вы знаете? Что они означают?
2**успешные коды
3** перенаправление
4**ошибки клиента
5** ошибки сервера

Назовите примеры текстовых форматов данных, которые используются при сериализации для передачи данных по сети:
Json
Xml
CSV
YAML

Что означает аббревиатура MVC?
Model View control - паттерн проектирования софта

Какие существуют подходы в обеспечении доступа к данным при данных условиях:
Данные не меняются в течение продолжительного промежутка времени
Кешировать данные

При каких условиях будет предпочтительным выбрать асинхронный способ взаимодействия между двумя системами вместо  синхронного?
Когда можно заранее подготовить ответ чтобы сократить latency
Чтобы улучшить отказоустойчивость и масштабируемость

Python. Приоритет MEDIUM

Каким ключевым словом задаётся функция?
Def

Каким ключевым словом задается метод?
Def

Какой стандартный декоратор нужно указать, чтобы сделать метод статическим?
@staticmethod

Какой пакет из стандартной библиотеки содержит в себе функции для работы с датами и временем? Это должен быть 1  пакет, он и со временем, и с датами работает.
Datetime

Какой пакет из стандартной библиотеки содержит в себе функции для работы с логами?
logging
